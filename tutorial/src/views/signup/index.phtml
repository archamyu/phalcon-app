<div class="row">
    <div class="col-lg-6">
        <div class="card shadow-sm mb-4">
            <div class="card-header">Sign up</div>
            <div class="card-body">
                <form action="/signup/register" method="post">
                    <div class="mb-3">
                        <label for="name" class="form-label">Name</label>
                        <input type="text" id="name" name="name" class="form-control" placeholder="Your name" required />
                    </div>
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" id="email" name="email" class="form-control" placeholder="you@example.com" required />
                    </div>
                    <button type="submit" class="btn btn-primary">Register</button>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-6">
        <div class="card shadow-sm">
            <div class="card-header">Users</div>
            <div class="card-body p-0">
                <div class="list-group list-group-flush">
                    <?php if (isset($users) && count($users)) { ?>
                        <?php foreach ($users as $user) { ?>
                            <div class="list-group-item d-flex align-items-center justify-content-between">
                                <div>
                                    <div class="fw-semibold"><?php echo htmlspecialchars($user->name, ENT_QUOTES, 'UTF-8'); ?></div>
                                    <div class="text-muted small"><?php echo htmlspecialchars($user->email, ENT_QUOTES, 'UTF-8'); ?></div>
                                </div>
                                <div>
                                    <a href="/signup/edit/<?php echo (int) $user->id; ?>" class="btn btn-outline-secondary btn-sm">Edit</a>
                                    <a href="/signup/delete/<?php echo (int) $user->id; ?>" class="btn btn-outline-secondary btn-sm">Hapus</a>
                                </div>
                            </div>
                        <?php } ?>
                    <?php } else { ?>
                        <div class="list-group-item">No users yet.</div>
                    <?php } ?>
                </div>
            </div>
        </div>
    </div>
</div>